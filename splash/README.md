# Splash

The splashscreen is generated from the index.html over the generate.py script. The script renders the page and stores three imageds in the ios/PersonalCredentialManager/Images.xcassets/Launch Screen folder